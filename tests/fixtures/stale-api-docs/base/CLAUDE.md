# CLAUDE.md

Plain-text report rendering library. Python 3.11+, type hints required on all
functions. Every public function carries a docstring. README.md documents the
public API and is the contract callers read — keep it in step with the code.
