.idea/
.DS_Store
.gg/
__pycache__/

# Studious local gate ledger (do not commit)
.studious/

# viva local session state — round files, answers, server.url (do not commit)
.viva/

# Disposable build scaffolding — lives on the branch, dies at closeout. What
# survives is the Done-means table + evidence in the PR body, and decision
# patches proposed to PRODUCT.md/DESIGN.md/CLAUDE.md. Build evidence itself
# never enters the repo: /build writes it to .studious/build-evidence/
# (covered by the .studious/ entry above), and /ship's PR body is the record.
/PLAN.md
docs/design/
docs/jig/demonstrations/

# A/B eval run output — regenerate with scripts/run_ab_eval.py (tests/ab/README.md)
ab-results/
