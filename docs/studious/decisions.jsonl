{"date":"2026-07-23","gate":"should-we-build","idea":"Fix epic-driver.js acceptanceRound: dispatch premortem-auditor for per-story pre-mortem registers, guard against an empty scope-check result","verdict":"BUILD","rationale":"Matches PRODUCT.md's #1 known problem (gate-contract regressions caught only by manual audit) directly and was cleanly scoped to one function."}
{"date":"2026-07-24","gate":"should-we-build","idea":"Absorb jig into the studious repo as a second plugin at plugins/jig/ — one diff domain, two separately-installable plugins via git-subdir","verdict":"BUILD SMALLER","rationale":"jig's last surviving boundary criterion is verifiably dead and the seam cost is being paid now, but the kernel is one diff domain plus one shipped SHA, not a versioning unification."}
{"date":"2026-07-24","gate":"should-we-build","idea":"Absorb jig into the studious repo as a second plugin at plugins/jig/ (git-subdir, per-plugin version tags, declared plugin dependencies); keep viva a separate repo","verdict":"BUILD SMALLER","rationale":"Supersedes the same-day entry: plugin.json dependencies and {plugin}--v{version} tags remove the unified-release-stream cost, and viva stays out because it publishes a versioned, tested headless contract while studious and jig couple through undocumented format conventions."}
