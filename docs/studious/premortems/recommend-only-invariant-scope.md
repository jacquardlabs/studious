# Pre-mortem — Rescope the recommend-only invariant

- Branch: fix/recommend-only-invariant-scope
- SHA: 69099b7
- Date: 2026-08-03

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | product | A future command self-declares "recommend-only" in its own body text but its actual tool grants or write calls modify external state anyway — bullet 1's predicate is honesty-dependent, not mechanically enforced. | The new command's `allowed-tools` frontmatter or body claims a write behavior that contradicts a "recommend-only" self-declaration nearby; `prompt-auditor` (`/gate-audit`) is the surface that would catch it. |
| 2 | product | A contributor misreads bullet 2's git-plumbing clause as licensing a new driver-like command to create branches/merge/manage worktrees outside `/work-through`'s specific "dispatched work under a human-approved plan" pattern. | A new command's body cites bullet 2 or "git plumbing" to justify a branch/merge/worktree operation that wasn't dispatched under a plan the human approved at that command's own front door. |
| 3 | technical | `bin/gate-ledger`'s `ensure_gitignore()` behavior changes (a different target file, a bypassed verb) without CLAUDE.md's bullet 2 citation (`bin/gate-ledger:220-231`) being updated — the citation is a snapshot, not a live contract. | A future diff to `bin/gate-ledger` touching `ensure_gitignore()` or its call sites, reviewed without a matching CLAUDE.md check. |
| 4 | technical | `docs/studious/decisions.jsonl`'s exception clause (CLAUDE.md bullet 2) and `reference/decision-journal-format.md`'s corrected prose drift apart if one is edited without the other — nothing mechanically ties them together. | Either file changes in a future PR without a corresponding edit to the other; `reference/decision-journal-format.md:92`'s own cross-reference line is the tripwire to check first. |
| 5 | product | #247, when resumed, cites bullet 3's one-off carve-out, but #247's actual (unbuilt) design needs something closer to bullet 1's self-declared-recommend-only shape instead — the carve-out was written to the *shape* #247's Q4 answer described, not against #247's real design. | #247's own future design-review, checking its carve-out citation against bullet 3's literal text rather than assuming the fit is automatic. |
| 6 | technical | `/gate-acceptance`'s incidental commit still has no *prescribed* path — bullet 2 conditions boundary coverage on "provided the write lands inside" `.studious/`/`docs/studious/`, but nothing stops a future `/gate-acceptance` run from committing its synthesis note outside that boundary, which would now be a real, uncovered violation rather than an accidental pass. | Any `/gate-acceptance` run's commit landing outside `docs/studious/`; `commands/gate-acceptance.md`'s own commit step still names no path. |
| 7 | product | Success metrics' merge-time predicate check depends on `/build` actually copying it into the PR body as part of its normal commit-and-report step — if `/build` treats that as optional prose instead of a required carry-forward, the check has no carrier again once this design doc dies at closeout. | The PR body this story's `/build` run produces — check for the merge-time predicate-check instruction verbatim, not paraphrased away. |
