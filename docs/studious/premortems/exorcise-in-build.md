# Pre-mortem — Exorcise inside /build (#318, seam 2)

- Branch: story/exorcise-in-build
- SHA: ea68b56
- Date: 2026-09-06

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Wrong diff scope reverts a sibling story's work. exorcise derives BASE from `@{upstream}...HEAD`, falling back to `main...HEAD`; a build worktree has no upstream and an epic story branch is cut from `epic/<slug>`, so the fallback scopes in every landed sibling's commits and intent-tracer `revert`s hunks no claim in this story reaches. | Read the dispatch prompt in `skills/build/SKILL.md` Step 3 and `exorcisePrompt` in `workflows/epic-driver.js`: both must set the branch upstream to the base before invoking and name the base as a branch, never a sha. In a run, the report header's `<BASE-short>..<HEAD-short>` must equal the story's own range. |
| 2 | technical | The verify re-run fails every `probe` item structurally. A fresh `--since` floor after exorcise postdates every probe artifact the earlier tasks committed (the #44 shape), so the FAIL branch fires on every plan with a probe item and the pass is always undone. | Step 3's `verify` call must reuse each task's own step 2.2 dispatch timestamp for `--since`; a run over a plan with one `probe` item must reach the PASS branch when exorcise touched nothing that item names. |
| 3 | technical | `evidence-capture` refuses the report as stale or the tree as dirty. The report is written to scratch before the `exorcise:` commit (mtime predates HEAD), or captured before the commit (working tree dirty), and the capture exits 1 — read as a task FAIL and routed into the Failure routine. | Step 3's order must be verify → commit → write report to scratch → capture; an exit 1 or 2 here routes to step 2.7's usage-error rule, never the Failure routine. `tests/python/test_build_exorcise_step.py` pins the order of those phrases. |
| 4 | technical | The driver's exorcise dispatch parks the story. A throw inside `runStory`'s existing `try` reaches `crashed` and parks under BLOCKED; a `blocked` return handled like the worker's parks INCOMPLETE. Either way a simplification costs the story, the one outcome the placement note forbids. | `tests/python/test_driver_exorcise_dispatch.py`: a rule that throws on `^exorcise:a$` must still land story `a`; the dispatch has its own try/catch and never feeds `park()`. |
| 5 | product | The Foreman starts reading diffs. The commit step tempts a `git diff` "just to write the message", or the subagent is asked to commit itself so `verify` runs inside its turn — either blurs the four-role framing the skill keeps. | Step 3's prose must source the commit message from the report's `Concepts removed:` line and commit on `verify`'s PASS; the subagent's boundary line must say it never commits. `git diff` must not appear in Step 3 outside the "never" clause. |
| 6 | product | `hold` findings silently vanish. The report is captured, but `/ship`'s evidence table quotes only what it is told to, and a `## Held` section nobody names reaches neither the PR body nor `/review`. | `skills/ship/SKILL.md` Step 1's table assembly must quote captured text artifacts inline (it does for `inspector:report` today); a run with a held finding must show the `## Held` section in the PR body. |
| 7 | technical | The intent restatement is wrong at epic scale. The driver hands exorcise the acceptance criteria but the recorded design doc is gitignored and may be gone by build time, so the Proposed design is absent and every hunk that served the design but not a literal criterion reads as unreached. | The driver prompt must treat a missing design doc as "criteria only", and the epic re-check (the project's own suite) must gate the commit; a FAIL rate near 100% on epic runs is the signal the intent is too narrow, not that the tool is wrong. |
