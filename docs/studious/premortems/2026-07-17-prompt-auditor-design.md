# Pre-mortem — Prompt auditor (gate-audit lane and deep-review prompts area for LLM-native repos)

- Design doc: docs/superpowers/specs/2026-07-17-prompt-auditor-design.md
- Branch: epic/expand-gate-coverage--prompt-auditor
- SHA: 809beb2
- Date: 2026-07-17

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Auditor renumbering drift: the 12→13 pre-mortem renumber and the "1–7, 9–12 / never 8 or 13" roster edits miss an occurrence in `commands/gate-audit.md` (evidence-log's "auditor 12", re-audit condition 3's count, carry-forward's "the ten"→"the eleven"), so a stale reference points at the wrong lane — the contract-drift class this very lane exists to catch, turned on itself. | Grep the merge diff of `commands/gate-audit.md` for every "auditor 12", "1–7, 9–11", "the ten"/"the eleven", "never 8 or 12" occurrence; any un-updated instance is the bug. |
| 2 | technical | Fail-open regression on the automated path: `promptMatch` is added to `routingScopeCheckPrompt` but `resolveAuditRoster`'s new branch (or a fixture) diverges, flipping the default so a missing/unparseable flag routes the lane OUT instead of IN, silently dropping coverage on the `/work-through` path. | Run `tests/python/test_audit_first_round_routing.py` for promptMatch true/false/absent and confirm absent → routed IN; verify `resolveAuditRoster` guards `promptMatch === false` exactly, mirroring the `depMatch` branch (driver ~line 293). |
| 3 | technical | Routing over-fire: the `*prompt*` filename pattern (`promptness.md` matches) and the repo-state-conditional `reference/**` pattern route the lane in on diffs with no real prompt surface, regressing gate latency on LLM-native repos and filling the ledger with self-skip-only dispatches (epic pre-mortem item 4; design Open Questions). | On a diff touching only `reference/` data tables or a `*prompt*`-named non-prompt file, confirm the lane dispatched-then-self-skipped; a cluster of skip-only rows in the gate ledger signals the signal list is too loose. |
| 4 | product | Gate-friction regression: in a fully LLM-native repo (this one) the seven-dimension rubric fires on most diffs and produces High findings routinely overturned at the challenge step, eroding the "lightweight and optional" principle the design leans on. | Track prompt-auditor's challenge-step overturn rate and gate latency on prompt-touching diffs; a rising overturn rate means the severity ladder or rubric breadth is miscalibrated (design's own failing-signal #4). |
| 5 | technical | Lane-boundary double-reporting: because the prompt/doc/security/architecture boundary lives only in dispatch-brief prose with no code enforcement, prompt-auditor and doc-auditor both report the same CLAUDE.md hunk, or security-auditor and prompt-auditor both claim one injection finding, yielding duplicate or contradictory gate rows. | On a diff touching CLAUDE.md or a prompt carrying an injection smell, scan the compiled gate report for the same file:line appearing under two lanes. |
| 6 | technical | Checklist not single-sourced in practice: `reference/prompt-checklist.md` is meant to hold the seven dimensions once, but `prompt-auditor` or `review-prompt-health` restates a dimension's probe list inline — recreating the duplication the lane flags (dimension 4 turned on itself). | Grep both new agents for inlined dimension probe lists that duplicate the checklist body — they must point, not restate; `scripts/check_references.py` must resolve the checklist link from both consumers. |
| 7 | product | Metrics-key contract drift silently drops trend data: `review-prompt-health` emits aggregate keys that don't byte-match the three dashboard rows wired into `commands/deep-review.md`, so the exact-key metrics-history append omits the prompts rows with no error — the "accretive posture becomes visible" success metric never materializes. | After a `/deep-review` run, diff the emitted metrics-snapshot keys against `Prompt files` / `Prompt duplication clusters` / `Prompt contract-drift findings`; a missing dashboard row means a key mismatch (design Open Question on metrics wording). |
