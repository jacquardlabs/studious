# Pre-mortem — Make the PR-time hook aware of recorded pre-mortem verdicts

- Design doc: docs/superpowers/specs/2026-07-09-premortem-hook-awareness-design.md
- Branch: epic/gate-ledger-robustness--premortem-hook-awareness
- SHA: 509d248
- Date: 2026-07-09

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | The silent-on-absence special-case is implemented too broadly — `cmd_status` suppresses the empty-verdict warning for all gates, not just `pre-mortem`, silently regressing the "audit never ran" / "acceptance never ran" warnings that are the ledger's original point (#27). | At audit time read `bin/gate-ledger` `cmd_status`: the empty-verdict branch (currently line ~153) must still emit "never ran" for `audit`/`acceptance` and skip only `pre-mortem`. Add/keep a test where a ledger file has no gates: it must still warn about audit+acceptance, and stay silent about pre-mortem. |
| 2 | technical | The read side ships with no producer, so no real `/work-through` run ever writes a `pre-mortem` key — the capability is dormant and `/gate-acceptance` can't answer "does it deliver?" affirmatively. | `grep -n "record --gate pre-mortem" workflows/epic-driver.js` — absent means the loop is open. Confirm the design's Open Question #1 (producer wiring) was filed as an immediate tracked follow-up issue, not deferred to "a future pass." |
| 3 | technical | Vocabulary drift: the producer records `CLEAR`/`REALIZED` but `cmd_status`'s registered pass-token for `pre-mortem` differs, so a `CLEAR`-at-HEAD is mis-flagged as "pre-mortem returned CLEAR." The recorded roll-up vocabulary has no single documented home (deliberately excluded from `reference/gate-vocabulary.md`). | Confirm the gates-table entry's pass token equals the producer's recorded pass token (`CLEAR`) exactly; test that a recorded `CLEAR` at HEAD produces NO message. Confirm `CLEAR`/`REALIZED` is documented in one place both sides cite (e.g. against `agents/premortem-auditor.md`). |
| 4 | product | Reusing the staleness message verbatim yields a non-actionable reminder for `pre-mortem`: "pre-mortem ran 3 commits ago — re-run before merging" tells a human at `gh pr create` to re-run a check only the epic finale can produce, which they cannot run by hand. | Render the staleness branch wording as it would appear for `pre-mortem` (older sha than HEAD on the epic branch). Judge whether "re-run before merging" misleads for a finale-only check, or needs pre-mortem-specific phrasing. |
| 5 | technical | Branch-slug collision: `branch_slug()` collapses `/` to `-`, so `epic/foo` and a hypothetical `epic-foo` share a ledger file; a `pre-mortem` verdict must be voided by the existing branch-mismatch guard on collision, not read blindly. | Confirm the new `pre-mortem` read path sits INSIDE the `branch_mismatch` guard (`bin/gate-ledger` ~line 134), so a mismatched stored branch zeroes the pre-mortem verdict exactly as it does audit/acceptance. |
| 6 | product | `CAN'T VERIFY`-only roll-up is undefined (Open Question #3): if the finale maps a register with only `CAN'T VERIFY` items (no `REALIZED`) to `CLEAR`/silent, a genuinely un-checkable cross-story risk passes the PR-time hook silently — re-opening the exact blind spot the feature exists to close. | Check how the producer's roll-up handles a `CAN'T VERIFY`-only register. A silent `CLEAR` there means the hook stays quiet on an unresolved risk; verify the build raised this against `agents/premortem-auditor.md`'s per-item contract rather than defaulting to silence. |
