# Pre-mortem — Split gate-audit.md's compile-time rules into reference/audit-compilation.md

- Design doc: docs/superpowers/specs/2026-07-21-audit-doc-split-design.md
- Branch: epic/perf-audit-followups--audit-doc-split
- SHA: 41a3cb0
- Date: 2026-07-21

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Build edits the wrong location because it trusted the doc's line numbers: `auditFanIn`'s pointer is cited at line 527 (actually 544), its call sites at 680/974 (actually 697/992), the whole file at 28,049 bytes (actually 28,642). A line-addressed edit lands in a code comment or the wrong function. | Locate the runtime pointer by its content string ("Read commands/gate-audit.md from the plugin root ... and apply ITS compilation rules"), never by line number. Grep confirms exactly one runtime pointer changes and the ~9 code-comment mentions of `gate-audit.md` in `epic-driver.js` stay untouched. |
| 2 | technical | Genericizing scope language ("this command" / "the report below" → "this audit round") silently drops or alters a compile rule rather than only rephrasing it — e.g. mangling the "you already have Read/Glob/Grep/Bash access to the full changeset" line the compiler uses to confirm a citation — so one caller's verdict path changes. | Word-diff the moved section against `gate-audit.md`'s original; confirm every genericized phrase preserves the same imperative and the critical-challenge confirm/downgrade/drop logic is substance-preserved. |
| 3 | technical | `tests/python/test_gate_audit_challenge_step.py` breaks: all 8 assertions extract text via regex between "consult it, don't restate it." and "Then compile a unified audit report", both anchors inside the moved section — so every assertion fails once `gate-audit.md`'s copy becomes a pointer. | Run `uv run --no-project --with pytest pytest tests/python -v`; the test's target read must be repointed to `reference/audit-compilation.md`, and the full suite (not just that file) must stay green. |
| 4 | technical | The new AC3 duplicate-copy regression test passes trivially or scans too narrowly: it asserts on a phrase that also appears in `gate-audit.md`'s untouched pointer paragraph, or scans only `check_references.py`'s four dirs (never `workflows/`), so it green-lights a real second copy. | Confirm the chosen distinctive line (e.g. "map each one's labels into the report's three tiers") appears exactly once repo-wide including `workflows/epic-driver.js`, and that a deliberately-injected second copy makes the test fail. |
| 5 | technical | `gate-audit.md`'s replacement pointer restates a compile rule instead of only naming the file (it is written "in the same voice as" the existing pointers), reintroducing a de-facto second copy and violating AC2/AC3. | Grep the replaced section for any severity-mapping, lane-state, or verdict-tier verb belonging to the moved rules; the pointer should name `reference/audit-compilation.md` and nothing more. |
| 6 | product | The two callers drift in report shape despite the "byte-identical structure" intent: the new file's two-caller header or routed-out fold-in reads as written-for-the-epic-path, so `/gate-audit`'s standalone session compiles a subtly different Summary/Critical/Important/Track report and `hooks/gate-reminder.sh` / `cmd_status` read an inconsistent verdict surface. | After the split, run one standalone `/gate-audit` compile and one `/work-through` story compile on the same reports; confirm identical section structure and the same three verdict tokens. |
| 7 | product | Folding "routed out" in as a compile-time lane state changes what the standalone `/gate-audit` user sees: the new file makes the standalone compiler emit a routed-out summary line it never emitted before (where routing was previously only a Launch-time skip note), so the report a human reads changes even though the verdict does not. | Compare a standalone `/gate-audit` report before/after on a changeset with a skipped auditor; confirm routed-out surfacing matches the prior Launch-time skip-note behavior, not a new compile-time line. |
