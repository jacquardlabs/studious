# Pre-mortem — /finish (PR evidence table, cctx footer, follow-ups, decision patches, report)

- Design doc: docs/design/finish-skill.md
- Branch: epic/m5-finish-skill--finish-skill
- SHA: ec14cee
- Date: 2026-07-12

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | The freshness-hold re-validator takes its floor from a moving target — the branch's current `HEAD`, its tip, or a branch-wide "last code commit" — instead of each evidence folder's own `manifest.json` `commit_timestamp`. This reproduces issue #44's bug shape one layer up: every task but the most recent fails the hold, so `/finish` refuses to assemble any PR body. This is the design's own named risk #1 and the whole reason Step 1's floor decision exists. | Read the new freshness re-validator script: the per-folder floor must be read from that folder's `manifest.json` `commit_timestamp`, not from `git rev-parse HEAD`, a `--since HEAD`, or the branch tip. A comparison against a single branch-wide commit for all folders is the bug. |
| 2 | technical | The hold's ancestor check (manifest `commit_sha` still reachable from `HEAD`) is implemented so an orphaned or rewritten commit silently passes — e.g. a string match, or treating an unknown/unresolvable SHA as "pass" rather than "fail." A stale evidence folder produced on a since-rebased commit (a real risk after a REPLAN hand-revision and rebuild) then gets promoted into the PR body as if fresh. | Confirm the check uses `git merge-base --is-ancestor <manifest-sha> HEAD` and treats a non-zero or unknown-SHA exit as FAIL that stops the run and names the task, never as a pass. Exercise a rebased/orphaned evidence folder and confirm `/finish` halts by name. |
| 3 | technical | The cctx step passes `--apply` inside its own default flow, or auto-applies a harvest on a bare "yes," instead of previewing and stopping. This breaks the acceptance criterion "always preview-confirms, never auto-applies" and principle "propose; never apply." | Grep the finished `skills/finish/SKILL.md` for `--apply`: every occurrence must be reachable only after an explicit, in-turn human confirmation; the default harvest path must be preview-only (`cctx harvest`, no `--apply`). Confirm the cctx-absent path prints the explicit "not installed" message and the install pointer, and skips to Step 3 with no error or silent gap. |
| 4 | product | Follow-up filing runs `gh issue create` before the human accepted that specific draft, or files the batch all-or-nothing rather than per-item. This is the design's named risk #3: an issue gets filed that no human confirmed, or a weak draft rides along to get a strong one filed. | Trace every `gh issue create` in the finished skill: each must be reachable only after a per-item accept/edit in the same turn; skipped drafts must be dropped, not filed. Any unconditional loop over the draft batch is the failure. |
| 5 | technical | Step 4 gains a convenience "apply this patch?" path that actually writes to `PRODUCT.md`/`DESIGN.md`/`CLAUDE.md` after a confirmation — silently softening the twice-stated "propose; never apply" that this step alone must honor even after an explicit "yes." Context-doc drift is the larger blast radius the design deliberately protects. | Confirm the finished Step 4 has no `Edit`/`Write`/`git apply`/patch invocation targeting the three context docs under any branch, confirmed or not — it prints proposed diff blocks and ends. |
| 6 | product | Verdict cleanup does the wrong git action for a token because the demonstration only exercised one path — the design's named risk #4. Concretely: deleting the branch or removing the worktree on `PR` (which must keep both) or on `KEEP` (keeps both), or leaving the worktree/branch behind on `MERGE`/`DISCARD` (which remove them). | The end-to-end demonstration must exercise more than one token; check the actual worktree presence, branch existence, and PR state after each against the Step 6 table row-by-row. Confirm the "design docs and PLAN.md die with the branch" cleanup commit runs before the git action for every token. |
| 7 | product | The required end-to-end demonstration is faked against a scratch path instead of a real committed `docs/jig/evidence/<date>-<task>/` folder — repeating the M4 shortcut that routed around issue #45 and never actually exercised a committed, freshness-held evidence folder. The freshness hold and raw-URL promotion would then be demonstrated against nothing real. | The acceptance evidence must show a genuine `docs/jig/evidence/<date>-<task>/` folder committed on the branch with a real `manifest.json`, not a `/tmp` or out-of-worktree scratch path; confirm `/finish`'s freshness hold and raw-URL construction ran against that committed folder. |
