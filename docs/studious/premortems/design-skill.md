# Pre-mortem — /design (batch interview, forks, sectioned doc, viva loop)

- Design doc: docs/design/design-skill.md
- Branch: epic/m2-design-skill--design-skill
- SHA: 9561083
- Date: 2026-07-16

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|-----------------|
| 1 | technical | The REVISED resume path (Step 6, case 3) keys detection off the doc already carrying a `## Revision History` heading, but no step in this design writes that heading. If Step 4's redraft never emits/updates `## Revision History`, a REVISED re-invocation collapses into case 1 (fresh session), runs the generic clear-state block, and destroys the prior round's `.viva/review-*.json` carry-forward — the exact M0 finding-3 trap the design claims to avoid, and epic risk #3. | In `skills/design/SKILL.md`, confirm Step 4 writes/updates `## Revision History` on any REVISED draft AND that Step 6 case-3's predicate matches that same heading. A diff where Step 6 reads `## Revision History` but Step 4 never writes it is the materialized bug; build test coverage must include a resume scenario, not only fresh-launch. |
| 2 | technical | Step 5 assumes `design-lint` honors a 0/1/2 exit-code contract, but issue #9's `design-lint` is not yet built. If #9 ships a different contract (0/nonzero only, or exit 2 on violations), `/design` mis-reads a usage error as a clean pass (skips the fix, launches viva on a bad doc) or a violation as a usage error (aborts spuriously). This is epic risk #1 at the exit-code level. | At #9 merge, diff `scripts/design-lint`'s real exit codes against the 0/1/2 contract this doc asserts. The required end-to-end demo (Operational readiness) must exercise a real nonzero-then-fix against #9's real script — a demo whose lint step is trivially clean against the current 0-exit stub does not prove Step 5's fix-before-viva branch. |
| 3 | technical | The `[intent]/[contract]/[experience]/[friction]` asking-tag and the fork marker both ride inside viva-qa's `hint`/`text` (Steps 2–3, an accepted schema gap). If viva-qa's card renderer truncates or reformats `hint`, the tag/fork signal is silently lost and the human answers an untagged question with no fork context. | Confirm `skills/design/SKILL.md` builds `hint` as `"[tag] …"` and that the installed viva-qa's `hint` rendering preserves the prefix verbatim. A viva-qa schema revision that adds a real tag field (the filed Open question) without `/design` migrating off the `hint` piggyback is the drift to watch. |
| 4 | technical | The same-tab viva-qa → viva handoff (Alternatives #1) assumes the QA server is still live when the review round launches after drafting. A long drafting gap, or viva#112's leak-reaper reaping the qa process, leaves a stale `.viva/server.url` pointing at a dead server; Step 6's launch fails or hangs. | Confirm Step 6's launch handles a stale/absent `.viva/server.url` via viva's own guard rather than assuming continuity from Step 2. A session where a real drafting gap separates the interview from the viva loop is where a dead-server launch surfaces. |
| 5 | product | The NEEDS RESEARCH path writes nothing to disk after the human answered up to ~14 interview questions across two rounds. A developer who invested both rounds and receives no doc may read it as wasted effort and distrust the gate — an adoption risk against the primary persona — especially if the terminal message doesn't name the specific unresolved fork concretely. | In the first real NEEDS RESEARCH session (or the required demo forced down this path), confirm the terminal verdict names the specific unresolved fork(s) that need a spike/prior-art/stakeholder input. A generic "needs more research" with no named fork is the materialized frustration. |
| 6 | product | `/design` emits contract-canonical headings (Step 4, option B) that DESIGN.md's Formatting section does not yet bless — it still names the handoff-literal Intent/Experience/Contracts/… seven. Until the flagged Open question is actioned, DESIGN.md (the interface contract) and `/design`'s real output diverge; a later `/deep-review interface` or the real `design-lint` could flag docs `/design` itself produced as non-conforming to the stale contract. | After this ships, check whether DESIGN.md's Formatting bullet was updated (via a `/finish` decision patch or `/deep-review interface`) to match `/design`'s actual output. An un-updated DESIGN.md alongside a `/design` that emits contract headings is the live interface drift. |
| 7 | technical | The story's required end-to-end demonstration depends on `design-lint` (#9) being real, but #8 and #9 are sibling stories with no enforced build order (Alternatives #4 deliberately rejects a hard block). If `/design` merges before #9, the demonstration runs its lint step against a stub that exits 0 unconditionally, so Step 5's fix-before-viva behavior is never actually proven end to end — the acceptance criterion is nominally met while its load-bearing check is inert. | At `/design`'s audit/acceptance, confirm the demo's `design-lint` invocation ran against #9's real script (a nonzero-then-fix cycle visible in captured evidence), not the M1 0-exit stub. A demo whose lint step is trivially clean is the gap. |
