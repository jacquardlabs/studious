# Pre-mortem — Narrow the finale's acceptance redo cascade

- Branch: epic/m1-gate-build-cost--acceptance-altitude-evidence
- SHA: 7f9ba6b
- Date: 2026-09-08

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|-----------------|
| 1 | technical | `finale:start-sha`'s `GATE_RESULT.sha` is an unvalidated string; an unresolvable or empty anchor makes `finale:acceptance-delta`'s diff empty/erroring, which is textually indistinguishable from a legitimately clean diff — a false-clean carry-forward on a `ready` epic. | Check the delta pass's prompt/schema explicitly returns non-SHIP when the anchor doesn't resolve or the diff can't be computed; confirm via a unit test feeding an unresolvable sha. |
| 2 | technical | The round-9 bug (carry-forward leaving `acceptanceFixCycles` at the raced run's stale value, firing a redundant third opus premortem dispatch) reappears if the implementation diverges from the design's explicit reset-to-0 instruction. | Confirm the new fixture asserting exactly two `finale:premortem` dispatches (not three) on the raced-acceptance-had-its-own-fix-cycle path. |
| 3 | technical | The "Acceptance redo fallbacks" report line never renders because the driver's own return object never gained the counter as a new key (the same way `degradedNarrowings` needed one at `:3736`) — a silent report-shape gap no fixture catches by default. | Check `reference/epic-orchestration.md`'s report-shape test actually asserts the new line prints when the counter is nonzero, and is omitted at zero. |
| 4 | product | A developer reads a nonzero "Acceptance redo fallbacks: N" and assumes the feature is broken, when the counter now counts every fallback including a correctly-reported concern (by design, per round 9's "full price paid" simplification) — normal operation, not a defect signal. | Confirm the report line's exact wording distinguishes "full price paid for any reason" from "the mechanism is broken," so the disclosure doesn't read as an alarm. |
| 5 | technical | `finale:acceptance-delta`'s sonnet tier under-detects a real acceptance-relevant regression in the delta (false-clean), silently carrying forward a stale SHIP with no fresh opus check — the accepted, bounded risk this design takes on. | No build-time detection possible; flag for `#279`'s eventual sonnet-tier evaluation to include this specific pass, not just the two audit-side pins it already covers. |
| 6 | product | At epic scale, the finale's PR opens automatically (per `#253`) on a carried-forward verdict, and the human merging it may never see the "Acceptance: carried forward, confirmed clean at `<sha>`" disclosure line before the PR lands. | Confirm the finale's closing report (with the new disclosure line) is actually surfaced to the human at or before PR-open time, not just logged and buried. |
| 7 | technical | `finale:start-sha` fires unconditionally on every finale (even when `auditFixCycles` ends at 0), adding latency to the common case with no reader ever consuming the value — a real, disclosed cost this design accepts rather than eliminates. | Once `cost-baseline`/`#384` lands telemetry, check the anchor's actual trigger-to-savings ratio matches the "small relative to the ~200k redo" framing, not a larger unaccounted overhead. |
