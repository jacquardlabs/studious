# Pre-mortem — Auditor 8 (accessibility) — dispatch the vendored-fallback path as a parallel Task

- Design doc: docs/superpowers/specs/2026-07-21-audit-a11y-parallel-dispatch-design.md
- Branch: epic/perf-audit-followups--audit-a11y-parallel-dispatch
- SHA: 105b6c1
- Date: 2026-07-21

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Model transition: the fallback runs inline at the *session* model today; the new agent pins `model: opus`. On a non-opus session this changes which model backs the accessibility check, so identical frontend diffs can yield different a11y findings than the prior inline path — a judgment-surface shift sitting on the epic's "cost/visibility only" line | Confirm `agents/accessibility-auditor.md` pins `model: opus`; note in acceptance that a non-opus session's a11y findings may differ from prior inline behavior (neutral-or-better, but not byte-for-byte identical judgment) |
| 2 | technical | The new agent reads its rubric via `${CLAUDE_PLUGIN_ROOT}/reference/accessibility-checklist.md` but omits the Glob fallback the sibling auditors use; an unresolved var reads an empty rubric in the consuming project and silently produces a clean pass | Grep `agents/accessibility-auditor.md` for the plugin-root read plus a Glob-fallback line matching `ux-reviewer.md`/`frontend-reviewer.md`; a bare `${CLAUDE_PLUGIN_ROOT}` with no fallback is the defect |
| 3 | technical | The Challenge-every-Critical "Non-code claims" bucket (gate-audit.md ~line 131) isn't updated to name `accessibility-auditor` alongside `web-design-guidelines`, so its blocking a11y Criticals fall through to code-content challenge logic that assumes a code citation and can be wrongly Dropped/Downgraded | Grep `commands/gate-audit.md` line ~131 for `accessibility-auditor` in the Non-code-claims enumeration |
| 4 | technical | The edit accidentally folds `accessibility-auditor` into the eleven-auditor `blockingLanes` list (or the AUDITORS roster), so re-audit narrowing starts tracking/dropping auditor 8 — which must stay outside that mechanism either way | Confirm `gate-audit.md`'s blockingLanes validation still lists exactly the eleven (1–7, 9–12) and the narrowing section still says auditors 8 and 13 sit outside it; confirm `epic-driver.js` AUDITORS still has 11 entries, no accessibility |
| 5 | technical | The AC2 rationale (unpinned-`main` WebFetch URL breaks reproducibility; unvetted third-party instruction source) lives only in the design doc, which isn't read at gate-runtime; if it doesn't land in `gate-audit.md`'s own auditor-8 skill-path text, a future maintainer sees the inline path as an unexplained inconsistency and "fixes" it | Grep `commands/gate-audit.md`'s auditor-8 entry for the reproducibility / unpinned-fetch reason the skill path stays inline — not just the design doc |
| 6 | technical | The stale `auditor 7` → `auditor 8` fix spans three files; a missed file or wrong-direction edit leaves a dangling cross-reference, and check_references.py won't catch a wrong-but-resolving number | `uv run --no-project python scripts/check_references.py` passes and `grep -rn "auditor 7"` returns nothing in `ux-reviewer.md`, `frontend-reviewer.md`, `accessibility-checklist.md` |
| 7 | product | The reworded Summary doesn't clearly distinguish the not-installed case (accessibility-auditor as a parallel lane) from the installed case (inline web-design-guidelines), so the persona sees an inconsistent report shape across two projects and can't tell which path ran | Read the reworded "note which path ran" instruction: it must name `accessibility-auditor` for the fallback and the `web-design-guidelines` skill for the installed path as two distinct, legible Summary lines |
