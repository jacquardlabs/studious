# Pre-mortem — scope delta for the epic path

- Branch: epic/driver-hygiene--scope-delta-measurement
- SHA: 7bd68e3
- Date: 2026-07-27

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | The design's "adds no dispatches of its own" rests on reading the file list off dispatches that already resolve it, but `resolveRoutingMatchFlags` returns only `{infraMatch, frontendMatch, depMatch, promptMatch, diffPath}` — it computes `git diff --name-only` internally and throws the list away. If the routing dispatch's JSON contract and result schema aren't both widened to carry `files`, every audit-side moment (build exit plus both audit fix cycles — three of the five) silently records `unmeasured` while the acceptance-side moments work fine, and the story looks measured. | Read `routingScopeCheckPrompt`'s returned-JSON spec and the matching schema in `workflows/epic-driver.js`: does the prompt name `files` in its `{...}` literal AND does the parse-side schema accept it? A prompt-only change with an unwidened schema drops the field. |
| 2 | technical | The Workflow script has no filesystem or exec access (`workflows/epic-driver.js:189`), so the driver cannot run `gate-ledger work-log` itself — the write must be typed by a dispatched model. If it lands as a new mechanical dispatch, the design's own "adds no dispatches" claim breaks against PRODUCT.md known problem #1 (cost). If it's folded into an existing fan-in prompt without a pinned, literal flag spelling, a model paraphrases the payload and the count is silently wrong with no read-back to catch it. | Grep `workflows/epic-driver.js` for new `agent(` call sites between the build worker and the audit round. If none, find which existing prompt carries the write and confirm it embeds the computed value and the exact flag verbatim (as `mergePrompt` does for `--phase done`), not a description of what to record. |
| 3 | technical | Two ledger-schema changes land in the same epic — this story's `declaredFiles`/`scopeDelta` (per-story *work* file, `work-set`/`work-log`) and #245's field at `.stories[].decisions` (epic *state* file, `epic-story-set`). The design asserts they cannot collide because the files and call surfaces differ. If either story instead reaches for a shared jq write helper or a shared arg-parsing case block in `bin/gate-ledger`, the second to merge silently drops the first's field. | After both land, `gate-ledger work-get` a story and `gate-ledger epic-get` its epic and confirm all three fields are present simultaneously. In the diff, check whether `cmd_work_set`/`cmd_work_log` and `cmd_epic_story_set` still own separate jq expressions. |
| 4 | technical | `bin/gate-ledger:456-466` hard-errors (exit 2) on any `--step build --outcome` outside `BUILT\|PAUSED\|ESCALATED\|HANDED-OFF\|SKIPPED`. If the count is appended to the outcome token to avoid adding a flag — or if a fixer's `work-log` call is given a composite outcome — the ledger write fails and the driver loses both the measurement and the step record for that moment. | Grep the driver and `bin/gate-ledger` for the delta value ever reaching `--outcome`. Confirm `work-log` gained its own dedicated flag, and that `tests/test_gate_ledger.sh` covers a `--step build` call carrying the new flag alongside a legal outcome token. |
| 5 | technical | The gate-flow exclusion names exactly two members (recorded `designDoc`, pre-mortem register), and Open question 4 concedes no enumeration was done. In this repo `docs/design/` is gitignored, so the `designDoc` half is a no-op that dogfood cannot exercise — it will only ever be tested in a consuming project that commits design docs. If it is implemented as a hardcoded path prefix rather than keyed off the recorded `.designDoc` value, it works here (vacuously) and fails there, giving every story a permanent one-file offset — the exact defect the rule exists to prevent. | Check whether the exclusion reads `.designDoc` from the work file or matches a literal `docs/design/` string. Then run one story and confirm its own register (`docs/studious/premortems/<slug>.md`, committed to the story branch by this gate) does not appear in its `scopeDelta`. |
| 6 | product | The two motivating stories (`verify-tier-grammar`, `evidence-path-integrity`) were both **parked**, never landed. `commands/work-through.md`'s terminal block is a fixed shape with `Needs you:` and `Landed this run:` as separate branches, and only the landed branch renders the phase trail as its whole line; a `needsYou` entry gets a parenthetical. If the count is wired only into the landed path, the persona sees no number for exactly the stories that motivated the feature. | Read the rendered terminal report from the first run with a parked story: does the `Needs you:` entry carry declared/outside/amended, or only the landed lines? |
| 7 | product | Metric 1 needs 5–10 stories; driver-hygiene ships 3, so the cohort must span runs and must be read by hand from work files. But every landed story writes `--phase done`, `gate-ledger gc` collects any file at a terminal phase, and `/studious-doctor` actively tells the user to run `gc` at >10 work files (`commands/studious-doctor.md:49`) — the same cohort size. The mitigation is prose ("hold off running `gc`") in a design doc that is deleted at closeout, so the product ends up recommending destruction of the only data round one exists to produce. | Between now and the cohort read, check `gate-ledger work-list` count against `/studious-doctor`'s output: does anything in the *shipped* surface warn before collecting a work file carrying an unread `scopeDelta`, or does the caution live only in the deleted doc? |
| 8 | product | Metric 1's own stated failure: every story reports zero outside its declaration, or every story reports its entire changeset. Either means the declaration is being ignored or is unusable, and nothing catches it early — the declaration is written by a worker, read by no gate (journey step 3 keeps `/gate-design-review` on the doc, not the declaration), and first inspected at the run summary after five moments have already been measured against it. | Read declared vs outside per story in the first run's terminal summary. All-zero or outside ≈ whole changeset on every story means the reference point never functioned; do not ship the deferred parking or retry-refusal consumers on that data. |
