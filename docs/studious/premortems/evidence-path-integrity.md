# Pre-mortem — evidence-capture leaves no orphaned artifacts and no cross-branch path collision

- Branch: `epic/m11-correctness-tail--evidence-path-integrity`
- SHA: 1a8450a
- Date: 2026-07-26

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | product | A legacy tier that is ambiguous resolves to "not found," and the persona cannot tell that designed refusal from a broken reader. In this repo tasks `1`–`4` each carry two branch-less manifests, so a legacy-only lookup returns nothing — and `/finish` emits the same *"evidence not found for item N"* line it emits when no evidence was ever captured. The persona reads a correctness fix as a regression the fix introduced. | Resolve a task whose only evidence is a branch-less manifest with a duplicated task id (`docs/jig/evidence/2026-07-12-*` both record `task: "task-1"`). Does the refusal distinguish ambiguous-legacy from no-evidence, or does one message cover both? |
| 2 | product | The `list` arity leaks another branch's task into a branch-scoped "where am I." A unique legacy match carries no `branch` key, so under rule 2 it resolves for *every* branch — `/coach` on a freshly-cut branch in this repo reports `2-retroactive-inspection` as captured. The design states two limits on the collision claim and the legacy tier's ambiguity in detail, but never this one. | Run the `list` arity against a fresh branch in a tree carrying branch-less manifests. Any row returned belongs to a branch that is not the one asked for. Also check `skills/coach/SKILL.md:59`'s replacement wording — does it promise "this branch's" evidence while the verb can return another branch's? |
| 3 | technical | The slug is claimed as reuse of `bin/gate-ledger:37`'s `branch_slug()`, but that function is bash and `scripts/evidence-capture` is Python. There is no shared home a cross-language rule can live in, so the story ships one rule with two implementations coordinated by prose — inside an epic whose stated goal is "no convention with a third uncoordinated copy," and beside sibling #206, which exists to retire exactly that shape. | Grep the diff for a Python `replace("/", "-")` (or equivalent) in `scripts/`. Is there a test asserting the Python output matches `bin/gate-ledger`'s `branch_slug()` over a set of branch names including `feat/foo`, `epic/x--y`, and a name with no slash? Absent such a test, the two can drift silently. |
| 4 | technical | The "one home for the tiebreak" rule has no mechanical guard. The three read-side call sites are prompt files (`skills/finish/SKILL.md:33`, `:86`, `skills/coach/SKILL.md:59`); nothing stops a later edit from re-explaining "newest by `captured_at`" or "unique legacy match" in prose, recreating the duplicated grammar this story was scoped to remove. | Grep `skills/finish/SKILL.md` and `skills/coach/SKILL.md` for `captured_at`, `legacy`, `newest`, or `branch-bearing`. Any hit means the round-3 one-home constraint regressed. Check `tests/jig/test_finish_skill.py` and `test_coach_skill.py` for an assertion that pins the absence, not just the presence of the call. |
| 5 | technical | `--force`'s clearing deletes previously-committed files, leaving the tree dirty with deletions rather than additions — a shape nothing has exercised, since no shipped caller passes `--force`. The exposure is one step downstream: the deletions sit uncommitted and the *next* capture refuses against the dirty tree. The design flags this as Open question 1 and specifies the demonstration; the risk is that the story ships a scratch-directory unit test instead, which never touches the index. | Look for a test or demonstration that runs `--force` over a *committed* `docs/jig/evidence/` folder and then checks `git status` after the deletions are committed. A test that only writes to a `--evidence-root` scratch path has not exercised this at all. |
| 6 | technical | The resolve verb publishes exit 1 for "well-formed query resolved to nothing," while `scripts/evidence-capture:29` currently documents exit 1 as "refused for a freshness reason (dirty tree, stale artifact, clock skew)." Sibling story #222 writes a normative DESIGN.md paragraph describing the 0/1/2 ladder across eight scripts; if this story overloads exit 1 without widening the docstring, the epic ships a documented contract that is false for its own newest verb. | Read `scripts/evidence-capture`'s docstring after the change: does its exit-code paragraph cover the resolve/list meanings, or still only the capture path? Cross-check against whatever DESIGN.md paragraph #222 lands — the two must not describe exit 1 differently for the same script. |
