# Pre-mortem — `gate-ledger evidence-list --dedupe`

- Design doc: docs/superpowers/specs/2026-07-21-evidence-list-dedupe-design.md
- Branch: epic/perf-audit-followups--evidence-list-dedupe
- SHA: e3aab04
- Date: 2026-07-21

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | `jq` absent at read time on a machine holding the branch's log makes `evidence-list --dedupe` fail closed to empty, so gate-audit auditor 5/13 and gate-acceptance's premortem dispatch get no citations at all instead of the raw log they get today — a narrow regression (safe/conservative: disclaimers become "attested," never wrong). | On a `jq`-less machine with a non-empty `.studious/evidence/<slug>.jsonl`, `gate-ledger evidence-list --dedupe` must exit non-zero with empty stdout, and `gate-audit.md`'s `test -s "$evidence_file"` must treat that as "no log" (no truncated/garbage block stamped into a dispatch). |
| 2 | technical | A malformed or partially-written line in the `.jsonl` breaks the `jq -s` slurp, so one bad record hides the entire otherwise-valid log from the deduped read. | New fixture appends one non-JSON line; assert `--dedupe` exits non-zero with empty stdout while the raw (no-flag) `cat` path still emits the file verbatim. |
| 3 | technical | The dedup pipeline reorders survivors alphabetically (`unique_by`) or lets a byte-identical same-second pair both survive (whole-object-equality), violating "at most one record per distinct command" or "chronological order among survivors." | Fixture asserts the deduped count equals the distinct-command count AND that the repeated command's survivor carries the **last**-appended `predicate.result`; confirm the built pipeline is the index-based `to_entries`/reduce form, not `unique_by`. |
| 4 | technical | Only one of `gate-audit.md`'s two `evidence-list` occurrences (the `$evidence_file` precompute and the in-prose "fall back to running `gate-ledger evidence-list` yourself") gains `--dedupe`, so a scratch-file `Read` failure silently regresses auditor 5/13 back to the full raw log — exactly the large-log case this story exists to shrink. | Grep `gate-audit.md`'s "Resolve the branch's evidence log" step: every `evidence-list` occurrence in it must carry `--dedupe`. |
| 5 | technical | `reference/evidence-format.md`'s "Consumers that must stay in sync" list still asserts gate-audit/gate-acceptance "stamp its **raw** output" and that `evidence-list` "is a plain passthrough" — the contract doc contradicts shipped behavior after the two dispatches switch to `--dedupe`. The design's evidence-format edit-plan (§4) covers a new `--dedupe` paragraph + a fixture line but does not name correcting this existing bullet. | At merge, grep the "Consumers that must stay in sync" section for "raw output" / "plain passthrough"; both must be reconciled with the deduped dispatch, not just the new paragraph added. |
| 6 | product | A future edit "helpfully" switches `handback.md` to `--dedupe` too, silently turning its complete-history manifest into a lossy current-state summary — the exact drift the §"three consumers" note exists to prevent. | `handback.md` step 2 must carry the explicit "keeps the raw form on purpose" sentence; its five `evidence-list --branch` calls (lines 45/127/142/143/144) must have no `--dedupe`. |
| 7 | product | Collapsing hides a historical fact an auditor genuinely needed (a command that flapped `PASSED` then later `FAILED`), if any dispatch instruction depended on seeing prior attempts of the same command rather than only current state. | The "Evidence log for this branch" citation prose in both `gate-audit.md` and `gate-acceptance.md` is unchanged and reads correctly against the deduped form; confirm no dispatch asks the auditor to reason over prior same-command attempts, only the current state of a claim. |
