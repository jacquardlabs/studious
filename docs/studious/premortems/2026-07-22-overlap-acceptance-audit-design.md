# Pre-mortem — Race finale acceptance against audit's fix-cycle loop

- Branch: epic/finale-gate-overlap--overlap-acceptance-audit
- SHA: 69865e6
- Date: 2026-07-22

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Raced acceptance dispatch records `gate-ledger record --gate acceptance` against the pre-fix sha, and the audit-triggered redo's own record never lands (redo agent dies or returns without recording) — the ledger keeps the stale pre-fix sha, so `finale.ready` and the PR-time hook read a verdict over a commit the fix mutated (epic premortem finding #1). | After a finale with `auditFixCycles > 0`, run `gate-ledger gate-get --branch epic/<slug>` — the acceptance record's `sha` sits *before* the audit fix commit rather than at/after it. |
| 2 | technical | The `epic-driver.js:1096-1097` comment ("audit's own fixers are no hazard here: this dispatch is created only after the audit finaleGate fully resolved") is left unchanged when premortem's dispatch moves to t=0, actively misleading the next reader about the hazard model (design mandates correcting it in the same change). | Grep the diff for the string "created only after the audit finaleGate fully resolved" surviving next to a premortem dispatch now created at t=0. |
| 3 | technical | The restructured block destructures `cycles: auditFixCycles` from `finaleGate('audit', ...)`; if that path does not populate `cycles` the way the acceptance path does, `auditFixCycles` is `undefined`, `> 0` is always false, and the discard-and-redo silently never fires. | A test with a mutating audit fix cycle asserts **two** `finale:acceptance` dispatches in the recorded `calls`; if only one appears, the trigger is dead. |
| 4 | technical | Premortem's second redo (the `acceptanceFixCycles > 0` re-check after the audit branch) misfires under composition — if `premortemPromise = freshPremortemPromise` or the `acceptanceFixCycles` rebind is dropped, premortem skips its needed redo or redoes against the wrong acceptance run (AC4 regression). | AC4 composition test: audit fix cycle, then a redo acceptance with its own fix cycle — assert premortem dispatched exactly **twice** total; one or three is the failure. |
| 5 | technical | The `(log(...), premortemDispatch())` comma-operator sketch is copied verbatim into the diff; the repo's eslint config flags the comma operator, failing `npx eslint@10.6.0 workflows/` (AC5) for a reason unconnected to the design. | `node --check` passes but eslint reports `no-sequences`; grep the diff for a comma expression inside the `auditFixCycles > 0` branch. |
| 6 | product | The two `log(...)` discard lines ("discarding the raced acceptance result", "discarding the raced premortem read") are dropped or reworded in implementation — the persona watching `/work-through` sees acceptance and premortem dispatch twice with no explanation, the exact "unexplained doubled cost" the epic premortem finding #2 names. | Grep a real finale transcript with a mutating audit fix cycle for both "discarding the raced ..." log lines; their absence is the regression. |
| 7 | product | Because this epic's own finale runs the *installed* marketplace plugin copy, not this worktree's edited copy (finding #4), a maintainer reading `finale-gate-overlap`'s own finale timing sees no speedup and wrongly concludes the change didn't work. | The `finale-gate-overlap` finale transcript still shows serial audit→acceptance dispatch ordering; only a later epic under the bumped installed version shows `finale:acceptance` dispatched before `finale:audit-compile`. |
