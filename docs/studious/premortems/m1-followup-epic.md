# Pre-mortem — M1: Remaining cleanup

- Epic: m1-followup
- Stories: context-doc-refresh (#26), coach-description-fix (#28), track-tier-cleanup (#29 scoped), vocabulary-single-source (#27)
- Branch: epic/m1-followup
- SHA: 6ca118d
- Date: 2026-07-12

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|-----------------|
| 1 | technical | `vocabulary-single-source` parses `DESIGN.md`'s Vocabulary table programmatically. `context-doc-refresh` runs concurrently (no dependency edge between them) and may touch that same table's structure/formatting while refreshing the doc. If the table's shape changes underneath the parser, it could break or — worse — silently misparse and still pass. | At `vocabulary-single-source`'s audit, confirm the parser's assumed table structure (column order, delimiter, heading) still matches `DESIGN.md`'s actual Vocabulary table at merge time on the epic branch, not just at story-branch HEAD before the merge. |
| 2 | product | Adding real Ruff config (`context-doc-refresh`) could surface pre-existing violations in files this epic doesn't otherwise touch (e.g. the scripts/tests from the M1 scaffold epic). If audit/acceptance judge against "whole repo is Ruff-clean" rather than the recorded criteria ("config exists and is valid"), the story could get stuck in an unwinnable fix-and-retry loop fixing unrelated pre-existing code. | Read `context-doc-refresh`'s audit/acceptance verdict rationale: it must cite the recorded acceptance criteria (config exists, valid TOML) as the bar, not an unstated expectation that the entire repo passes `ruff check` clean. |
| 3 | technical | `track-tier-cleanup`'s source issue (#29) lists 6 checklist items; this story is deliberately scoped to only 3 of them (dead assignment, finish's milestone wording, Pillar 2 heading). A build agent reading the raw issue body (not just the recorded story criteria) could reasonably try to tackle all 6, including the ones needing human judgment (committing the external handoff doc) or a live check (plugin loader rendering) that a static build task can't resolve. | Check the story's diff against its recorded criteria only. If it touches anything related to committing the handoff document, CI wiring, or loader-verification, that's over-scope — the Not-here boundary should have prevented it, and its presence is a finding, not a bonus. |
| 4 | technical | `track-tier-cleanup` and `vocabulary-single-source` both touch `tests/test_discipline_skill.py`. The recorded dependency edge (`vocabulary-single-source` deps on `track-tier-cleanup`) is meant to prevent a merge conflict — if the scheduler dispatches them concurrently anyway, the second merge could silently clobber the first story's fix or produce a real conflict. | Check `gate-ledger work-log` timestamps for `m1-followup--vocabulary-single-source`'s first dispatch against `m1-followup--track-tier-cleanup`'s landed timestamp — the former must not start before the latter lands. |
