# Pre-mortem — reconcile design-lint's section-name schema to design-doc-contract.md's seven

- Design doc: docs/design/design-lint-reconcile.md
- Branch: epic/m2-design-skill--design-lint-reconcile
- SHA: c18639c
- Date: 2026-07-16

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Check 3's three grounding buckets (verbatim `PRODUCT.md` blockquote / tree-resolvable path / `(qN)` ruling) are OR'd, so a `Problem & persona` section carrying any stray real file-path inline-code span passes even when its persona/JTBD claim is fabricated or absent from `PRODUCT.md` — the check degrades to "some checkable token appears here," not "the persona is real." | At build time add a fixture whose `Problem & persona` names a persona not in `PRODUCT.md` but cites one unrelated real repo path; it must FAIL Check 3. If it passes, the bucket-OR is too loose. Inspect the new Check 3 cases in `tests/test_design_lint.py` and the acceptance logic bucket (c) vs (a)/(b). |
| 2 | technical | The acceptance criterion's closing demo — `design-lint --doc docs/design/design-skill.md` exits 0 clean — cannot run from this worktree (that file lives on sibling branch `epic/m2-design-skill--design-skill`) and may exit non-zero once the branches reconcile if `design-skill.md` carries an 8th heading, a `Proposed design` under `MIN_ARTIFACT_SPANS`, or a `Problem & persona` blockquote that is not a verbatim `PRODUCT.md` substring. | Build-phase evidence must capture the actual transcript with `exit=0`, run after rebasing/merging onto a tree that holds both files — asserted-as-done is not evidence. Re-run and attach output; do not accept a claim without the transcript. |
| 3 | technical | Check 1's exact `== 7` rule (no missing, no extra heading) is stricter than `reference/design-doc-contract.md`, which does not forbid an 8th section. A future real `/design` doc that legitimately splits out an appendix would fail a lint that `/design`'s own Step 5 forces to pass — reintroducing the exact "guaranteed failure, no escaping edit" loop this story exists to eliminate. | Grep the built section check for the `== 7` / unrecognized-heading rejection. Confirm behavior on an 8th recognized-but-extra heading; if it hard-fails, weigh against the contract's silence on an 8th section (flagged in the doc's own Open questions). |
| 4 | product | The remap silently changes what a lint failure means without any migration signal, while `DESIGN.md`'s "Design doc structure" line (line 56-57) still lists the OLD handoff names (`Intent, Experience, Contracts, Approach...`, "5-8 sections"). A developer following that stale guidance authors the wrong headings and gets a failure whose cited authority still names the very headings they used. | Check `DESIGN.md` line 56-57 and `templates/design-doc.md` at build/merge time. If either still lists the handoff names or the 5-8 range, the developer-facing guidance contradicts the enforced vocabulary — the story's Out of scope defers the `DESIGN.md` fix to `/deep-review interface`, so this contradiction ships. |
| 5 | technical | Check 2 retargets at `Proposed design` (a longer, denser section) with an unchanged `MIN_ARTIFACT_SPANS = 3` floor; a narrative-heavy design can accumulate 3 incidental backtick spans that are not real artifact citations, so the concreteness guarantee weakens exactly where the section grew. | Add a build-phase fixture whose `Proposed design` is prose plus exactly 3 non-artifact inline-code spans (e.g. `` `true` ``, `` `null` ``, a backtick-quoted prose word); observe whether it passes. The exact floor is deferred to build as a constant — confirm it was actually reconsidered, not copied. |
| 6 | technical | The linter's own failure message ("`... does not match jig's canonical section vocabulary — see DESIGN.md's Design doc structure`") still points the reader at the `DESIGN.md` line that lists the pre-remap names. Post-remap, a real violation's guidance string misdirects the author to stale names. | Grep the patched `scripts/design-lint` for the "see DESIGN.md" message string; confirm it was updated to cite `reference/design-doc-contract.md` (or that `DESIGN.md` was corrected in lockstep). If neither, the message sends authors to the wrong seven names. |
