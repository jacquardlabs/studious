# CLAUDE.md (fixture -- disposable-widget-app)

A minimal, disposable target-project fixture, built solely to host this
story's required demonstration (4): "A `DESIGN GAP` from missing probe
tooling, demonstrated, not just asserted" (`docs/design/plan-skill.md`,
Operational readiness). Not a real project.

## Code conventions

- **JavaScript** -- a small Node/Express app serving one HTML page.
- **Linter** -- none configured.

### Quality gates

- **Tests** -- `npm test` (Jest unit tests only; see `package.json`).
- **Deliberate deviations** -- none.

No scripted-browser/UI-driving tool is named anywhere in this file. This
omission is deliberate and load-bearing for the demonstration: Step 1b's
first checkable signal (a `CLAUDE.md` naming one explicitly) must come up
empty here.
