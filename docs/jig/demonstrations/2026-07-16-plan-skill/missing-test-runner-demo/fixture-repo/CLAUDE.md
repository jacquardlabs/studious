# CLAUDE.md (fixture -- disposable-report-formatter)

A minimal, disposable target-project fixture, built solely to host this
fix cycle's missing-test-runner `DESIGN GAP` demonstration (acceptance
gate's ONE OBSERVATION on story `plan-skill`: pre-mortem register item
#3's detection hint). Not a real project.

## Code conventions

- **Python** -- a small script that formats a CSV into a Markdown table.

No "Tests" row, no baseline/test command, and no linter named anywhere in
this file. This omission is deliberate and load-bearing for the
demonstration: Step 1b's test-runner read ("read the target project's own
`CLAUDE.md` for its stated baseline/test command... never guess a test
runner and never hardcode one") must come up empty here.
