# Pre-mortem — M1: Repo & plugin scaffold

- Epic: m1-scaffold
- Stories: scaffold-skeleton (#5), discipline-skill (#6), marketplace-registration (#7)
- Branch: epic/m1-scaffold
- SHA: a8503b1
- Date: 2026-07-11

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|-----------------|
| 1 | technical | `marketplace-registration` (#7) rests on "a first tagged version of jig existing" — cutting a git tag is a human action outside this epic's automated scope. If the driver treats the `scaffold-skeleton → marketplace-registration` dependency edge as sufficient on its own, the story will stall indefinitely waiting on a tag that never gets created, rather than parking cleanly for the human. | Check the story's recorded status when `scaffold-skeleton` has landed but no tag exists on the repo (`git tag --list`). It should read `parked` with a reason naming the missing tag, not `pending`/stuck silently or `blocked` with no explanation. |
| 2 | technical | `scaffold-skeleton` (#5) has design and design-review trimmed — the one real design decision it still makes (how `skills/` is structured) has no design-review gate to catch a mistake. This project has first-hand evidence of exactly this failure mode: viva's own issue #101 is "viva-qa and viva-diff never register as skills — one directory per skill required," caused by nesting a skill inside another skill's directory. `discipline-skill` (#6) is a *second* skill written into the same `skills/` tree `scaffold-skeleton` creates — if it isn't given its own top-level directory, it repeats viva's exact bug. | At `scaffold-skeleton`'s audit, confirm `skills/` contains one top-level directory per user-invoked skill (`design/`, `plan/`, `build/`, `finish/`, `coach/`), each with its own `SKILL.md` — no skill nested inside another skill's directory. At `discipline-skill`'s build, confirm its file lands in its own top-level directory (or the model-invoked-skill equivalent location), not inside `skills/design/` or any other user-invoked skill's directory. |
| 3 | technical | `discipline-skill` (#6) writes into the same `skills/` tree `scaffold-skeleton` (#5) creates — the dependency edge must actually be respected by the scheduler (no concurrent write to the same directory before #5 lands), not just recorded as data the driver ignores. | Check `gate-ledger work-log` timestamps for `m1-scaffold--discipline-skill`'s first dispatch against `m1-scaffold--scaffold-skeleton`'s landed timestamp — the former must not start before the latter lands. |
| 4 | product | Nothing in this epic has real functional code — stub scripts (`scripts/plan-lint`, `scripts/design-lint`) and stub `SKILL.md` files are the intended, correct output at this milestone. If `audit`/`acceptance` judge these against an implicit expectation of full functionality rather than the recorded acceptance criteria (stub existence + correct shape), every story could get stuck in an unwinnable fix-and-retry loop trying to "complete" work that M2-M6 own. | Read each story's audit/acceptance verdict rationale: it must cite the recorded acceptance criteria (stub existence, correct shape, correct frontmatter) as the bar, not an unstated expectation that `plan-lint`/`design-lint` actually lint anything yet. |
